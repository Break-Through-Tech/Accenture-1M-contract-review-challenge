[pytest]
testpaths = tests
# Lets tests import clean_text / prepare_cuad directly, the same way
# prepare_cuad.py imports clean_text when run as a script.
pythonpath = scripts
markers =
    slow: needs the prepared files in data/processed (run scripts/prepare_cuad.py first)
